# Audit — 2026-08-30/31: "are we building on bad code?"

Kevin's brief (2026-08-30, unattended overnight): design tests to find breakage, unfinished and unused code
after the day's migrations (C8–C17, G1) and the earlier data deletions; use cheaper models where results are
verifiable; plug in data through real paths if needed. Everything below ran against the TEST database or
read-only against live; live's census was untouched by every run.

## 1. What ran

| Pass | Tool | Model | Result |
|---|---|---|---|
| Link & door integrity | `tests/audit/links.js` (script) | — | 209 targets, 80 routes, 18 redirects: **0 unresolved** |
| Dead / unfinished code | `tests/audit/deadcode.js` + Sonnet triage (`TRIAGE_static`) | Sonnet | 116 items triaged: 5 real bugs · 39 unfinished · 26 dead · 32 external/OK · 14 tested-but-caller-missed |
| Coverage gaps | `tests/audit/coverage.js` | — | 247 of 401 backend routes and 32 of 62 frontend routes have no harness reference |
| Route crawl | `tests/audit/crawl.js` (Playwright, navigation-only, every non-GET blocked) | — | 495 page loads across 9 user types: 9 routes flagged (§5) |
| Golden setup | `tests/verify_golden_setup.js` (new harness, in the suite) | Fable | **37/37** after four product fixes (§2) — a city CAN be configured end-to-end through its own screens |
| Spec / architecture drift | Opus agent (`docs/audit/2026-08-31_spec_and_architecture.md`) | Opus | 92 items: HIGH 47 · MEDIUM 35 · LOW 10 — incl. **21 parent/child invariant violations (9 HIGH)** and one direct request INSERT (§3 B0/B); stale copy fixed where mechanical |

## 2. Real defects found — FIXED in this pass

| # | Defect | Where | Fix |
|---|---|---|---|
| 1 | **A proposal applied after its domain changed overwrote the newer values.** Locking the state proposes template merges for domains that already exist; the merge is computed at proposal time. The screens then set the payment clock / clarification / waiver on; applying the (stale) proposals wrote `enabled:false` back. On live, the two proposals pending since 08-26 would have done the same to whatever Kevin configured after them. | `routes/configFreshness.js` apply | Template proposals are re-merged against what is stored NOW (city's values win, template adds evidence); any other proposal whose base moved is refused 409 `STALE_PROPOSAL` with wording. |
| 2 | **`POST /onboarding/fees/test-result` always 500.** `updated_at`/`completed_at` are TIMESTAMP columns; the `datetime('now')` shim renders TEXT. Every "It behaves correctly / Something is off" click on the fee test tab failed. | `routes/onboarding.js` | `now()` for the two timestamp columns. |
| 3 | **Fee-waiver section could never be configured.** Readiness tested `feeWaiverPolicy.enabled`, which no screen has set since Fee rules absorbed waivers (F2); the screen writes the approval module. Go-live could never reach ready. | `services/jurisdictionProfile.js` | The section's signature and configured-test read the approval module the screen writes. |
| 4 | **A prerequisite marked done still blocked its dependents** (email marked → "When the system warns you" stayed waiting): the hub's dependency pass ran before marks were applied. | `services/setupHub.js build()` | Readiness computed through the chain with marks counting (never over needs_attention). Hub harness C4 added. |
| 5 | **`ack_email` switch was a no-op** — saved by System Notifications, never read; "Disabled" still sent the acknowledgement. | `services/requestCreate.js` | Gated on the setting. |
| 6 | **Email test never recorded** — the hub's Email row reads `email_last_test_ok`; the test route never wrote it, so the row could never reach ready. | `routes/integrations.js` | Written on a successful send. |
| 7 | `/setup/email` and `/magic` sat on "Loading…" forever for staff whose user type gets a 403. | `EmailConfigPage`, `MagicPage` | Say so. |
| 8 | Help assistant's app context described eleven deleted admin tabs as current (it is told never to invent nav items, so it would confidently direct staff to screens that don't exist). | `services/helpAgent.js` | Rewritten to the hub + `/setup` screens. |
| 9 | Staff error text "Set one up under Fee Configuration" (screen deleted). | `routes/feeEstimates.js` | Names Fee rules. |
| 10 | `verify_magic_reset` crashed in ~half of full-suite runs: the reset's private pools were terminated by its own `pg_terminate_backend`; unlistened pool error killed the process. | `services/magicDemo.js` | `quietPool()`; 3/3 passes since. |

Also deleted (dead, documented): `pages/SetupPage.js` (retired wizard, unmounted), `components/FeeSandboxPanel.js` (only the wizard used it), `components/ui/TaskPoolSection.js` (zero references). Stale copy fixed in `DOMAIN_MAP.md`, `SPEC_jurisdiction_configuration.md`, `SPEC_user_type_model.md`, `AI_DATA_TOUCHPOINTS.md`, `PROMPT_INJECTION_DEFENSE.md`.

## 3. Real defects found — NOT fixed (Kevin's call)

| # | Defect | Why it waits |
|---|---|---|
| A | **MFA is non-functional.** `mfa_mode` (User Authentication Setup) can be set to Required; `LoginPage` has the 6-digit UI; but `POST /auth/login` never checks `mfa_mode` and `POST /auth/mfa/verify` does not exist. The setting has zero effect on login. | A security feature to design and build (or remove the setting + UI until built), not a patch. |
| B0 | **Parent/child invariant — 21 violations, 9 HIGH** (`docs/audit/2026-08-31_spec_and_architecture.md` §1e). The citizen-facing cost-estimate, balance-due, final-cost and clarification notices and the fee-waiver denial email quote the CHILD's component-suffixed number (`routes/feeEstimates.js:256,601,678`, `services/clarificationAction.js:55`, `routes/requests.js:646`); `fee_waiver_status` is written on one row and read on another (`routes/requests.js:606` vs `feeEstimates.js:256`) so a granted waiver can be invisible to the notice; and BOTH auto-close sweeps (`services/feeNonpayment.js:54-63`, `services/clarificationTimeout.js:83-92`) close the parent while `applyStageTransition` has no cascade — children stay live with claimable tasks (the comment at `clarificationTimeout.js:79-82` describes a cascade that does not exist). The fix idiom (`numberJoin`/`numberExpr`, `feeEstimates.js:104-107`) already exists in the same file. | **FIXED 2026-08-31 (the 9 HIGH):** cascade close/reopen in `applyStageTransition`, `requestScope.parentFacts` on the estimate panel, three fee notices, financial profile and clarification letter, waiver written on the parent — `verify_parent_child_facts` 19/19. The 12 MEDIUM/LOW sites remain (listed in SPEC_parent_child_lifecycle §6.5). |
| B | **Architecture violation:** `services/connectors/nena911.js:52` inserts a request directly (`req-911-proactive`, born at stage `delivery`) — bypasses the one creation helper and is a manufactured pseudo-request (ARCHITECTURE items 1, 3, 5, 7). `reportEngine` carries `SYS-%` exclusions to keep it out of every metric. Two more pseudo-requests survive as data (`LIBRARY`, `SYS-TEMPLATE-SAMPLES`). | Needs a decision on what a proactive-disclosure anchor should be (a Notification? a library table?). |
| C | `POST /fee-estimates/request/:id/final-payment/record` — a second, no-longer-called final-payment door that updates the estimate without writing the `fee_payments` ledger (the live door does). Still reachable. | Delete or route through the ledger; the reissue harness pins it, so a deliberate change. |
| D | `app_url` is read (onboarding review deep-link) but nothing sets it; the link is a bare path to a retired `/setup?phase=` route. | Onboarding review flow is retired with the wizard — decide whether the routes go too. |
| E | Two golden-harness findings about the product's own doors: 14 policy settings have **no suggested value** (fees `Estimate-Fee.addt/.fcom`; exemption `Denial.*`; redaction `Redaction.r1/r2`; branches `Estimate-Fee.dwv/.wrev`, `Master.s1/.s2`; disposition `Disposition.fmt`, `Records-Search.inst`) — the harness confirmed them with type-appropriate values and every confirm was accepted, but a human on the screens sees no default; and `branches`, `disposition`, `template_import` have no screen at all (attest went through the raw endpoint). | Feeds the Jurisdiction Configuration retirement plan (§6 of the 08-30 handoff). |
| E2 | Spec drift beyond today's edits (full list: `docs/audit/2026-08-31_spec_and_architecture.md` Part 2): `SPEC_fees_estimates_payments.md:25-43` carries a ⛔ warning block that code fixed in July (aggregation exists; `verify_nonpayment_scope` is registered) and still calls the built MRR hub a blocker; `SPEC_jurisdiction_configuration.md` never mentions the hub or the attestation fold; `SPEC_setup_hub.md`'s slice rows quote historical counts (noted inline now). | Doc slices for Kevin's redaction/jurisdiction planning. |
| F | `SPEC_auth_security_platform.md:6` still describes the retired function-role/permission-role token; `CONFIG_FRESHNESS_DESIGN.md` names `/rule-updates` as a nav item. | Doc edits outside today's scope; noted. |

## 4. Unfinished (built server-side, no door) — 39 items, the useful groups

- **Requests:** reopen (`POST /requests/:id/reopen`, Director-gated, built 07-29, no button); clarification preview.
- **Tasks:** suggest / manual assign; close-approval queue (explicitly "do not build in v1").
- **Dispositions:** pipeline panel, withdrawal communication, hold/lift, installment request — all tested, none with a control on `DispositionsPage`.
- **Clocks:** overdue feed (for the tickler), start-one-clock, **statutory extension** (36-assertion harness, no control anywhere).
- **Taxonomy:** category edit/delete, many-to-many department and repository links (spec promises CRUD; the editor PATCHes single values).
- **Config:** freshness sources (dormant by decision 06-24), promote-now, jurisdiction-profile status/sync/ledger, redaction-config editor (backend verified, UI not built).
- **Settings with no setter:** `mass_redaction_window_start`, `mass_redaction_nightly_budget`, `erp_base_url`, `erp_webhook_secret`, `portal_search_ai_routing`, `portal_search_judge` — all read with defaults, none settable from any screen.

## 5. Route crawl

495 page loads — every non-parameter route (55, incl. the tabbed screens) as one active user of each of the
9 user types, navigation only, every non-GET `/api` request blocked. **No page errors, no console errors, no
failed page loads, no unexpected writes-on-load for any user type.** The 9 flagged routes are one class:

| Route | What the crawl saw | Status |
|---|---|---|
| `/setup/email` | 7 non-admin types: `GET /integrations` 403 → "Loading…" forever | **fixed** (says the screen is the System Administrator's) |
| `/magic` | 7 non-admin types: `/magic/status` 403 → blank / "Loading…" | **fixed** (says so) |
| `/setup/ai-configuration` (+3 tabs) | 7 non-admin types: `GET /integrations` 403 → the strip renders, the key/deployment status shows as unset with a "could not be read" note | acceptable; could name the permission |
| `/jurisdiction-config` | team_staff: `policy-settings` / `go-live` 403 → blank | screen slated for retirement; not fixed |
| `/release-review-power` | team_staff: blank (no task list for a non-reviewer) | cosmetic |
| `/setup/fee-law?tab=test` | POSTs `/fee-law/preview` on load — a non-persisting price computation (the test tab's live preview) | by design; noted so the crawl's "blocked write" isn't misread |

Admin, Director and Senior Legal saw every screen render with content. Re-run: `node tests/audit/crawl.js out.json && node tests/audit/crawlsum.js out.json`.

## 6. Coverage

Route files with every route untested: `parentFinance`, `workflow`, `tickler`, `redactionConfig`, `settlement`, `workflowModel`, `semanticSearch`, `reports`, `clarificationPolicy`, `classify`, `help`, `feeSandbox`. Live, frontend-called features with no harness: AI reporting, semantic search. Frontend: the whole `/setup/*` family, `/login`, `/portal*`, `/my-tasks`, `/org`, the task screens — the golden harness now covers the `/setup/*` backends end-to-end, not their UIs.

## 7. Dead routes (safe to delete; not deleted tonight)

`estimate-profiles/:id/actuals` (POST) and `DELETE /estimate-profiles/:id`; `workflow-model/node/:id`; `DELETE /decision-reasons/:id`; `tasks/:id/release-review`; `dispositions/:id/hold` (GET); `mrr/item/:id/activity/:a/start`; `parent-finance/:id/{quoted-shares,netting,adjustments,settlement}`; `notifications/unread-count`; `public/{refine-search,native-search,sources}` (retired chat-first panel); `redaction-templates/:id/apply`; `fee-profiles/{extract,preview}`; `POST /clarification-policy`; `jurisdiction-profile/{eligibility,approval-modules (GET/PUT)}`; `fee-estimates/request/:id/payment-timeline`.

## 8. Data vs code — the answer to Kevin's third question

The "setup not complete / fee won't calculate" symptoms on live are **mostly data**: fee rules at 0 of 13 city choices and no schedule version, 0 redaction rules, payment clock off, two proposals waiting since 08-26. The golden harness proves the code path from empty → ready → priced estimate works once the doors are walked — with the four fixes in §2 it did not before (fee-waiver could never configure; a marked prerequisite never released; the stale proposals would have undone the clock and clarification switches; the test-outcome click always failed). Configuring live is Kevin's walk-through, through the same screens; §3-E lists the settings the screens offer no default for.

## 9. How to re-run

`cd backend && npm test` (the golden harness is in the suite, last before the reset). Audit tools: `backend/tests/audit/README.md`.
