# Build Priority Summary — every [NOT BUILT] across all 12 domain specs
Compiled 2026-07-08 from the verified domain specs. Ranked for a time-constrained path to a sellable core. Sizes are directional.

## Tier 1 — Close the core single-record demo loop
1. **Record-search task screen** (D7) — engine fully exists; demo-minimal screen = request context + layered search + select + mark found/complete. *Medium.*
2. **Redaction task → workspace wiring** (D8/D4) — workspace exists; minimally, a redaction task click should open the job/workspace, not generic request detail. *Small.*
3. ~~**Populate estimate profiles** for the top ~10 record types (D6)~~ **[BUILT 2026-07-14 — `SPEC_fees_estimates_payments.md` §2, `verify_estimate_profiles` 15/15]** — ten expert seeds (police block + permits/minutes/email), seeded through the real `PUT /api/estimate-profiles/:id` path and carried in `seed_fixture.sql`. **All ten now assess AUTOMATED**; verified live on `2026-000048` (a real submission spawned *"Review auto-generated estimate"*). Seeds are stamped **PROVISIONAL** — a records clerk should confirm the numbers. **This task is what exposed the §552.261(a) labor-bar defect** (§8b): the automated path would have emitted an unlawful charge at scale.
4. ~~**Fee-waiver approval task routing** (D4)~~ **[BUILT 2026-07-09, verified 2026-07-11]** — intake `fee_waiver_requested` → `onIntake` spawns a team-agnostic `fee_waiver` task scoped to `FEE_AUTHORITY`; visible in an approver's pool (not a non-approver's); resolved by `POST /:id/fee-waiver-decision` (grant/deny → task done + history). Interim role `FEE_AUTHORITY` pending the Finance rename (item 9). *(This doc predates the 07-09 build.)*
5. ~~**Explicit found/not-found resolution states** (D7)~~ **[BUILT 2026-07-14 — `SPEC_record_search_task_screen.md` §5d-1, `verify_search_intent_gate` 37/37]** — the enforcement half of `BACKLOG` R9. R9 captured the requestor's intent (`search_more` / `no_match_search` / `not_searchable`) and only *displayed* it; the un-gate is now built — the searcher answers each duty-carrying description with `records_added` or `nothing_further` (**note required**, because that sentence closes a description the requestor considers open). An **unresolved** intent now blocks `found` with **422 `UNRESOLVED_SEARCH_INTENT`**. **The hole it closed:** the requestor's own portal picks already satisfied the old "≥1 Include in Response" gate, so a request the requestor explicitly asked us to search further could be fulfilled from their selection alone and **closed as complete while they still considered it open**. `SEARCH_INTENT_RESOLVED` is deliberately **not** effort-trail evidence — otherwise the claim would evidence itself and clear the no-records gate too.

## Tier 2 — High-value, pre-sales-polish
6. **Fee-choice intake (default-forward)** + rich quick-reply widget, chat AND form (D1 §5). *Medium.*
7. **Notification model + nullable task-request link** (D4/D9) — kills the SYS-IMPORT pseudo-request wart at the root. *Medium.*
8. **My Tasks restructure** — **[8a BUILT 2026-07-15]** task-centric page: per-**task-type** boxes, no-empty-boxes, pool section, Queued/In-Process labels, notifications area, null-request tasks visible (D4 §5; verified live). Health deferred to #13. **[8b BUILT 2026-07-15]** `BACKLOG` R10 — returned-for-rework: a general task `returned` flag (`markTaskReturned`) that renders "URGENT CORRECTIONS REQUIRED" on the author's task row + pushes a `work_returned` notification (both pull and push, via item 7). Wired for **redaction** returns (+ author-side screen banner) and **fee-objection** rejections (push only); clarification is a future customer. `verify_returned_rework` 13/13.
9. **Role catalog reconciliation** to one task-routing set + FEE_WAIVER_APPROVER→Finance rename (D4 §8). *Small-medium.*
10. **Legal Review task wiring; Legal Redaction path** for sensitive types (D4 §7; verify sensitive flag). *Small-medium.*

## Tier 3 — Ambitious subsystems (post first customer signal)
11. **Parent/child always-wrap + MRR processing flow** — **DESIGNED 2026-07-13: `SPEC_parent_child_lifecycle.md`** (field split, five-axis vocabulary, roll-up, record-hold vs clock-hold, withholding log, additive migration; grounded in a TX + 6-state legal review). Measured: 125 requests, **0 children ever created**, so the backfill is clean; the existing row becomes the **child** and keeps its id (8 work-level FK tables untouched), 7 money/clock tables repoint to a new parent. Then the full RM flow (Multi-Record Estimate/Search tasks, non-system contributor email link, Verify step, roll-up, early-release, HIGH PRIORITY report). ~~Two live bugs must be fixed first~~ — **both FIXED 2026-07-13** (`a9f8d29` ghost stage; `9ba8f32` raw stage writes). **RATIFIED + MERGED 2026-07-16:** architecture item 1 ratified; `SPEC_parent_child_lifecycle.md` is now the single binding spec (§12 of `SPEC_tasks_roles_mrr_fees.md` reduced to a pointer stub), with the citizen/fee layers folded in as §13, the MRR staff workflow as §14, toll attribution as §4.2.1, and child routing decided as **suggest-and-confirm** (§14.2). **No decisions remain — this is now work.** Sequence: (1) purge the 129 demo requests (Kevin, 2026-07-16 — all test residue, no migration warranted); (2) the backfill — now **trivial: 0 rows to convert**; (3) the portal emitting children (+ retire the dead `mrrChoice`); (4) §4.2.1 `source_request_id` toll attribution. *(An earlier draft listed a `tickler.js` deposit-sweep rewrite as step 1 — that was **already shipped 2026-07-14**, `a68df67`. §11.1 was stale and I propagated it.)* **Blocked on design, not decisions:** the MRR hub (§14.3, UI rule). *Large.*
12. **MRR item-by-item intake** in the portal agent (D1 §6). *Medium.*
13. **Workload health scoring** (exponential lateness formula, time budget input) + composite scores + management dashboard + AI reporting hooks (D4 §4, D11). *Medium-large.*
14. **Taxonomy variant level + auto-discovery groupings** (D3 — Kevin's open design). *Large.*
15. **Sources screen presentation redesign** (D9 — decision pending). *Medium.*
16. Remaining: commercial approval `[deferred — customer demand]`; fee-waiver-denial response window `[legal research first]`; help-agent retrieval upgrade (corpus now exists); production email connector; auto-publish automation (currently deliberate manual); per-task screen team variants.

## Data tasks (no code)
State jurisdiction profiles beyond TX+2; estimate profiles (item 3); redaction rule library per jurisdiction (AI-propose exists).

## Hardening (pre-production, from D12)
Password KDF (replace SHA-256/static salt) · TLS · MFA · secrets + volume encryption · backups · logging hygiene · vector payload encryption + index minimization · third-party pen test · (consider) gatekeeper agent.
