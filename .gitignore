backend/.env
backend/data/
backend/node_modules/
frontend/node_modules/
frontend/build/
*.log
.DS_Store

# Root .env (Docker secrets — never commit)
/.env

# Demo connectors live in their own repo
demo-connectors/
backups/

# rendered page images (regenerated on demand)
uploads/processed/
uploads/
*.bak
docker-compose.yml.bak.*
demo_sources/

# Seeded design-canvas payloads (2 MB generated wrappers — re-seed from the .dc.html sources)
docs/mockups/**/setup-configuration-hub.html

# design-canvas bundles (2.5 MB seeded editors) — sources are the .dc.html + canvas.json
docs/mockups/hub_links/*/*.html
!docs/mockups/hub_links/*/*.dc.html
docs/mockups/fee_waiver_content/*.html
!docs/mockups/fee_waiver_content/*.dc.html
